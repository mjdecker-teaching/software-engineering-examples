# Simple Setup
version: 2
jobs:
    build:
      docker:
        - image: srcml/ubuntu_x86_64_base
      steps:
        - checkout
        - run: cd ci/my_stl
        - run: mkdir build
        - run: cd build
        - run: cmake ..
        - run: ctest
